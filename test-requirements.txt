# Minimal dependencies needed for running unit tests in CI
# These are the core packages required for imports to work

pytest>=8.0.0
pytest-timeout>=2.2.0

# Core dependencies needed for module imports
numpy==2.2.4
opencv-python-headless==4.11.0.86
onnx==1.17.0
onnxruntime  # Lightweight CPU version
Pillow>=10.0.0
tqdm>=7.0.0
psutil>=7.0.0
scipy==1.15.2
scikit-learn>=1.3.0

# Needed for FFmpeg operations
ffmpeg-python>=0.2.0

# Minimal ML deps for basic functionality
# Note: CI will install CPU-only versions to save time and space
torch>=2.0.0,<3.0.0
torchvision>=0.15.0

# Skip heavy optional dependencies for CI:
# - insightface (requires building from source, model downloads)
# - mediapipe (large binary)
# - gradio (UI framework, not needed for tests)
# - training deps (accelerate, transformers, diffusers, peft, etc.)
